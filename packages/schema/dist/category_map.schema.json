{
  "$comment": "GENERATED by newsab_schema.export — do not edit. Definition site: packages/schema/newsab_schema/ | blueprint value_chain.md stage 3.5 / analyze refactor D-c | status: settled",
  "$defs": {
    "CategoryMerge": {
      "additionalProperties": false,
      "description": "One merge group: every member category is counted as ``canonical``.\n\n``rationale`` is one auditable sentence on why these are the same concept — it\ntravels with the artifact so a reviewer can challenge the merge, not reconstruct it.",
      "properties": {
        "canonical": {
          "title": "Canonical",
          "type": "string"
        },
        "members": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Members",
          "type": "array"
        },
        "rationale": {
          "$ref": "#/$defs/LangText"
        }
      },
      "required": [
        "canonical",
        "members",
        "rationale"
      ],
      "title": "CategoryMerge",
      "type": "object"
    },
    "LangText": {
      "additionalProperties": false,
      "description": "A free-text field with its language, per §1.6's hard rule.",
      "properties": {
        "lang": {
          "maxLength": 16,
          "minLength": 2,
          "title": "Lang",
          "type": "string"
        },
        "text": {
          "minLength": 1,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "text",
        "lang"
      ],
      "title": "LangText",
      "type": "object"
    },
    "Provenance": {
      "additionalProperties": false,
      "description": "``{skill_version, model_id, run_id, timestamp}`` (§3.2, §4.1).\n\n``model_id`` is ``None`` for purely deterministic producers (A1, scripts): recording\n\"no model was involved\" is itself an audit fact, and D10 makes it a meaningful one.",
      "properties": {
        "model_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Model Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "pattern": "^[A-Za-z0-9]+-\\d+\\.\\d+\\.\\d+$",
          "title": "Skill Version",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id",
        "timestamp"
      ],
      "title": "Provenance",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "One topic's category normalization, pinned to the answers run it was built on.\n\nPer question a list of merge groups; a question absent from ``merges`` maps every\ncategory to itself (the identity is the default, so an empty map is a valid map).\nMerge-only invariants, per question: no member appears in two groups, and no\ncanonical is itself a member of another group — projection is a single lookup,\nnever a chain.",
  "properties": {
    "answers_run_id": {
      "minLength": 1,
      "title": "Answers Run Id",
      "type": "string"
    },
    "merges": {
      "additionalProperties": {
        "items": {
          "$ref": "#/$defs/CategoryMerge"
        },
        "type": "array"
      },
      "title": "Merges",
      "type": "object"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "question_set_version": {
      "minLength": 1,
      "title": "Question Set Version",
      "type": "string"
    },
    "topic_id": {
      "title": "Topic Id",
      "type": "string"
    }
  },
  "required": [
    "topic_id",
    "question_set_version",
    "answers_run_id",
    "provenance"
  ],
  "title": "CategoryMap",
  "type": "object"
}

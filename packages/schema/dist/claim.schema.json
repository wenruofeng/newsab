{
  "$comment": "GENERATED by newsab_schema.export — do not edit. Definition site: packages/schema/newsab_schema/ | blueprint §4.5 | status: settled",
  "$defs": {
    "ClaimType": {
      "description": "Three provenance paths, kept visually distinct on the page (§3.2, §4.5).\n\n``corpus_reading`` exists because the page must answer *what* the two sides say and\nnot only *how often* they say it.  That answer is read out of the source text; no A1\nmetric measures it.  Giving\nit its own type is how the cost stays visible: it may carry no ``computed_from`` and no\nquantifier, so it can never be mistaken on the page — or by a checker — for a number.",
      "enum": [
        "source_claim",
        "corpus_aggregate",
        "corpus_reading"
      ],
      "title": "ClaimType",
      "type": "string"
    },
    "LintVerdict": {
      "description": "Outcome of one mechanical lint (§3.3 S8-L0).",
      "enum": [
        "pass",
        "flag",
        "fail"
      ],
      "title": "LintVerdict",
      "type": "string"
    },
    "MultiLangText": {
      "additionalProperties": false,
      "description": "A label carried in several languages at once (topic titles, concept labels).",
      "properties": {
        "values": {
          "additionalProperties": {
            "type": "string"
          },
          "minProperties": 1,
          "title": "Values",
          "type": "object"
        }
      },
      "required": [
        "values"
      ],
      "title": "MultiLangText",
      "type": "object"
    },
    "Provenance": {
      "additionalProperties": false,
      "description": "``{skill_version, model_id, run_id, timestamp}`` (§3.2, §4.1).\n\n``model_id`` is ``None`` for purely deterministic producers (A1, scripts): recording\n\"no model was involved\" is itself an audit fact, and D10 makes it a meaningful one.",
      "properties": {
        "model_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Model Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "pattern": "^[A-Za-z0-9]+-\\d+\\.\\d+\\.\\d+$",
          "title": "Skill Version",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id",
        "timestamp"
      ],
      "title": "Provenance",
      "type": "object"
    },
    "QuantifierCheck": {
      "additionalProperties": false,
      "description": "The record that a quantifier in the claim text was bound to a number (§4.5).",
      "properties": {
        "bound_to": {
          "pattern": "^[a-z_]+=-?\\d+(\\.\\d+)?$",
          "title": "Bound To",
          "type": "string"
        },
        "lint": {
          "$ref": "#/$defs/LintVerdict"
        },
        "phrase": {
          "minLength": 1,
          "title": "Phrase",
          "type": "string"
        }
      },
      "required": [
        "phrase",
        "bound_to",
        "lint"
      ],
      "title": "QuantifierCheck",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "One reader-facing sentence, with its provenance path (§4.5).\n\nThe three ``claim_type`` values are different kinds of promise: ``source_claim`` says\nsomeone said this and points at the sentence where they said it; ``corpus_aggregate``\nsays our sample looks like this and points at the A1 metric plus illustrative\nsentences; ``corpus_reading`` says *we read the source text and this is what the two\nsides' answers are* — evidence-bound, but measured by nobody.  Conflating the first two\nis how \"reported parity\" gets mistaken for \"real parity\" — the failure mode §0.2 names\nin the competitor review.  Conflating the third with either is how an editorial\njudgement acquires the authority of a statistic.",
  "properties": {
    "angle_id": {
      "title": "Angle Id",
      "type": "string"
    },
    "claim_id": {
      "title": "Claim Id",
      "type": "string"
    },
    "claim_type": {
      "$ref": "#/$defs/ClaimType"
    },
    "computed_from": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Computed From"
    },
    "evidence": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Evidence",
      "type": "array"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "quantifier_check": {
      "anyOf": [
        {
          "$ref": "#/$defs/QuantifierCheck"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "text": {
      "$ref": "#/$defs/MultiLangText"
    },
    "topic_id": {
      "title": "Topic Id",
      "type": "string"
    }
  },
  "required": [
    "claim_id",
    "angle_id",
    "topic_id",
    "text",
    "claim_type",
    "evidence",
    "provenance"
  ],
  "title": "Claim",
  "type": "object"
}

{
  "$comment": "GENERATED by newsab_schema.export — do not edit. Definition site: packages/schema/newsab_schema/ | blueprint §4.3 | status: settled",
  "$defs": {
    "Concept": {
      "additionalProperties": false,
      "description": "One canonical concept and every surface form merged into it (§4.3).",
      "properties": {
        "concept_id": {
          "title": "Concept Id",
          "type": "string"
        },
        "label": {
          "$ref": "#/$defs/MultiLangText"
        },
        "merged_by": {
          "$ref": "#/$defs/MergedBy"
        },
        "surfaces": {
          "items": {
            "$ref": "#/$defs/ConceptSurface"
          },
          "minItems": 1,
          "title": "Surfaces",
          "type": "array"
        }
      },
      "required": [
        "concept_id",
        "label",
        "surfaces",
        "merged_by"
      ],
      "title": "Concept",
      "type": "object"
    },
    "ConceptSurface": {
      "additionalProperties": false,
      "description": "An original wording that was merged into a canonical concept.\n\n``example_obs`` keeps the merge auditable: a reviewer can open the observation the\nsurface came from and judge whether the merge was fair.",
      "properties": {
        "example_obs": {
          "title": "Example Obs",
          "type": "string"
        },
        "lang": {
          "title": "Lang",
          "type": "string"
        },
        "text": {
          "minLength": 1,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "text",
        "lang",
        "example_obs"
      ],
      "title": "ConceptSurface",
      "type": "object"
    },
    "MergedBy": {
      "additionalProperties": false,
      "description": "Provenance of one merge decision.\n\n``run_id``/``skill_version`` identify the run that *produced* this record — an\nincremental re-normalisation restamps them even for concepts it carried forward\nunchanged. ``first_run_id`` is the run in which the merge was first *decided*;\n``None`` means \"this run\" (the concept is new). Without it, \"this merge decision\nis from an earlier round\" is lost on every re-run (non-negotiable 9).",
      "properties": {
        "first_run_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "First Run Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "minLength": 1,
          "title": "Skill Version",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id"
      ],
      "title": "MergedBy",
      "type": "object"
    },
    "MultiLangText": {
      "additionalProperties": false,
      "description": "A label carried in several languages at once (topic titles, concept labels).",
      "properties": {
        "values": {
          "additionalProperties": {
            "type": "string"
          },
          "minProperties": 1,
          "title": "Values",
          "type": "object"
        }
      },
      "required": [
        "values"
      ],
      "title": "MultiLangText",
      "type": "object"
    },
    "Provenance": {
      "additionalProperties": false,
      "description": "``{skill_version, model_id, run_id, timestamp}`` (§3.2, §4.1).\n\n``model_id`` is ``None`` for purely deterministic producers (A1, scripts): recording\n\"no model was involved\" is itself an audit fact, and D10 makes it a meaningful one.",
      "properties": {
        "model_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Model Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "pattern": "^[A-Za-z0-9]+-\\d+\\.\\d+\\.\\d+$",
          "title": "Skill Version",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id",
        "timestamp"
      ],
      "title": "Provenance",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "The versioned surface -> concept map for one topic.\n\nA1 joins on ``ontology_version`` (§4.3): re-merging concepts produces a new ontology\nversion and therefore a new feature matrix, rather than silently changing yesterday's\nnumbers.",
  "properties": {
    "concepts": {
      "items": {
        "$ref": "#/$defs/Concept"
      },
      "title": "Concepts",
      "type": "array"
    },
    "ontology_version": {
      "minLength": 1,
      "title": "Ontology Version",
      "type": "string"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "topic_id": {
      "title": "Topic Id",
      "type": "string"
    }
  },
  "required": [
    "topic_id",
    "ontology_version",
    "provenance"
  ],
  "title": "ConceptOntology",
  "type": "object"
}

{
  "$comment": "GENERATED by newsab_schema.export — do not edit. Definition site: packages/schema/newsab_schema/ | blueprint §2.4 / §3.3 S7 / §4.6 | status: draft",
  "$defs": {
    "AngleSection": {
      "additionalProperties": false,
      "description": "One angle card's worth of page (§2.4's three layers), as claim references.\n\nLayers are stored as claim ID lists rather than as prose, so \"what does the reader see\nbefore clicking\" is a machine-checkable question.  S8 uses it: an overview layer\nholding a claim with no evidence is the single most damaging defect this format has.",
      "properties": {
        "angle_id": {
          "title": "Angle Id",
          "type": "string"
        },
        "angle_type": {
          "$ref": "#/$defs/AngleType"
        },
        "cards": {
          "items": {
            "type": "string"
          },
          "title": "Cards",
          "type": "array"
        },
        "detail_claims": {
          "items": {
            "type": "string"
          },
          "title": "Detail Claims",
          "type": "array"
        },
        "gate_status": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Gate Status"
        },
        "headline": {
          "$ref": "#/$defs/MultiLangText"
        },
        "overview_claims": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Overview Claims",
          "type": "array"
        },
        "rank": {
          "minimum": 1,
          "title": "Rank",
          "type": "integer"
        }
      },
      "required": [
        "angle_id",
        "rank",
        "angle_type",
        "headline",
        "overview_claims"
      ],
      "title": "AngleSection",
      "type": "object"
    },
    "AngleType": {
      "description": "The §3.3 A1 angle types, plus the special-cased blind spot (§4.1).\n\nThese are **labels**, not scan domains (R-3): every bare/attr feature is\nscanned identically, the structural types describe what a feature compares, and\n``shared_ground`` / ``co_silence`` name interval *readings* (consensus, joint silence)\nrather than shapes.  ``co_silence`` is an additive extension of the locked vocabulary:\nboth sides leaving a controlled-vocabulary cell empty is data, not a gap (D5).",
      "enum": [
        "salience",
        "problem_definition",
        "consequence",
        "voice_structure",
        "actor_role",
        "stance",
        "terminology",
        "shared_ground",
        "co_silence",
        "blind_spot"
      ],
      "title": "AngleType",
      "type": "string"
    },
    "MultiLangText": {
      "additionalProperties": false,
      "description": "A label carried in several languages at once (topic titles, concept labels).",
      "properties": {
        "values": {
          "additionalProperties": {
            "type": "string"
          },
          "minProperties": 1,
          "title": "Values",
          "type": "object"
        }
      },
      "required": [
        "values"
      ],
      "title": "MultiLangText",
      "type": "object"
    },
    "PageNote": {
      "additionalProperties": false,
      "description": "A caveat that must reach the reader, and where the obligation came from.\n\n``source`` is load-bearing: ``founder_annotation`` is something a human wrote at a\ngate, ``dossier_caveat`` is something an agent carried forward on its own authority,\nand ``sampling`` is derived from the corpus.  Collapsing them would let an agent's\nself-assessment acquire a human's authority (AGENTS.md §8).",
      "properties": {
        "note_id": {
          "minLength": 1,
          "title": "Note Id",
          "type": "string"
        },
        "scope": {
          "minLength": 1,
          "title": "Scope",
          "type": "string"
        },
        "source": {
          "pattern": "^(founder_annotation|dossier_caveat|sampling)$",
          "title": "Source",
          "type": "string"
        },
        "text": {
          "$ref": "#/$defs/MultiLangText"
        }
      },
      "required": [
        "note_id",
        "source",
        "text",
        "scope"
      ],
      "title": "PageNote",
      "type": "object"
    },
    "Provenance": {
      "additionalProperties": false,
      "description": "``{skill_version, model_id, run_id, timestamp}`` (§3.2, §4.1).\n\n``model_id`` is ``None`` for purely deterministic producers (A1, scripts): recording\n\"no model was involved\" is itself an audit fact, and D10 makes it a meaningful one.",
      "properties": {
        "model_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Model Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "pattern": "^[A-Za-z0-9]+-\\d+\\.\\d+\\.\\d+$",
          "title": "Skill Version",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id",
        "timestamp"
      ],
      "title": "Provenance",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "The whole comparison page for one topic, before localisation (§3.3 S7).\n\nIt holds structure and references only.  The sentences live in ``Claim`` records and\nthe side prose in ``AngleCard`` records, because those are the units that carry\nprovenance — a container that also held text would be a place for an unanchored\nsentence to hide.",
  "properties": {
    "a1_run_id": {
      "minLength": 1,
      "title": "A1 Run Id",
      "type": "string"
    },
    "corpus_run_id": {
      "minLength": 1,
      "title": "Corpus Run Id",
      "type": "string"
    },
    "g2_run_id": {
      "minLength": 1,
      "title": "G2 Run Id",
      "type": "string"
    },
    "notes": {
      "items": {
        "$ref": "#/$defs/PageNote"
      },
      "title": "Notes",
      "type": "array"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "s6_run_id": {
      "minLength": 1,
      "title": "S6 Run Id",
      "type": "string"
    },
    "sections": {
      "items": {
        "$ref": "#/$defs/AngleSection"
      },
      "minItems": 1,
      "title": "Sections",
      "type": "array"
    },
    "standfirst_claims": {
      "items": {
        "type": "string"
      },
      "title": "Standfirst Claims",
      "type": "array"
    },
    "title": {
      "$ref": "#/$defs/MultiLangText"
    },
    "topic_id": {
      "title": "Topic Id",
      "type": "string"
    }
  },
  "required": [
    "topic_id",
    "g2_run_id",
    "s6_run_id",
    "a1_run_id",
    "corpus_run_id",
    "title",
    "sections",
    "provenance"
  ],
  "title": "EditorialPage",
  "type": "object"
}

{
  "$comment": "GENERATED by newsab_schema.export — do not edit. Definition site: packages/schema/newsab_schema/ | blueprint value_chain.md stage 4 / V-3 | status: settled",
  "$defs": {
    "FindingDelta": {
      "additionalProperties": false,
      "description": "The effect size a finding asserts, with its posterior interval.\n\nSince the analyze refactor: ``value`` is the observed quantity and\n``[lo, hi]`` its posterior interval under the pseudo-count prior, both anchored to\nthe finding's asserted top categories.  Quantities: ``consensus_dominance`` (mean of\nboth sides' shares of the common top answer), ``divergence_share_gap`` (mean absolute\nown-minus-other share gap over the two sides' top answers), ``addressed_rate_diff``\n(attention gaps).  Pre-refactor runs carry ``top_category_share_diff`` bootstrap\nspreads.  Shown to readers as ``value [lo, hi]`` — never the conservative end alone.",
      "properties": {
        "group_a": {
          "title": "Group A",
          "type": "string"
        },
        "group_b": {
          "title": "Group B",
          "type": "string"
        },
        "hi": {
          "maximum": 1.0,
          "minimum": -1.0,
          "title": "Hi",
          "type": "number"
        },
        "level": {
          "exclusiveMaximum": 1.0,
          "exclusiveMinimum": 0.0,
          "title": "Level",
          "type": "number"
        },
        "lo": {
          "maximum": 1.0,
          "minimum": -1.0,
          "title": "Lo",
          "type": "number"
        },
        "quantity": {
          "minLength": 1,
          "title": "Quantity",
          "type": "string"
        },
        "value": {
          "maximum": 1.0,
          "minimum": -1.0,
          "title": "Value",
          "type": "number"
        }
      },
      "required": [
        "quantity",
        "group_a",
        "group_b",
        "value",
        "lo",
        "hi",
        "level"
      ],
      "title": "FindingDelta",
      "type": "object"
    },
    "FindingKind": {
      "description": "The shapes a Q×A finding can take (value_chain.md \"The Q×A model\").\n\nSince the analyze refactor the emitted kinds are ``consensus`` /\n``divergence`` / ``attention_gap``, and a question emits at most one of them.  An\nattention gap (qa-0.4.0 semantics) asserts one side barely addresses the question\n(rate below the ``silent_max_rate`` threshold) while the other side addresses it\nsubstantially more — the blindspot-like reading; a mere rate difference between two\nsides that both plainly speak is not a finding.  It is first-class (it can carry an\nangle) and its angle always marks the quiet side; when the quiet side has zero\naddressed clusters the finding sets ``total_silence`` and is worded as\nannotation-layer silence.  ``blindspot`` and ``coverage_gap`` are no longer emitted\nbut stay members — historical runs on disk reference them (append-only).",
      "enum": [
        "consensus",
        "divergence",
        "attention_gap",
        "blindspot",
        "coverage_gap"
      ],
      "title": "FindingKind",
      "type": "string"
    },
    "FindingStrength": {
      "description": "The analyze stage's verdict on one finding at the current corpus size (V-3).\n\nComputed by plain Python against versioned thresholds; the writer cannot move it.\n``unsupported`` findings may not be asserted in prose or charts.",
      "enum": [
        "supported",
        "weak",
        "unsupported"
      ],
      "title": "FindingStrength",
      "type": "string"
    },
    "GroupAnswerStats": {
      "additionalProperties": false,
      "description": "One side's answers to one question, in countable form.\n\n``clusters_total`` is the side's independent-cluster denominator (non-negotiable 3);\nevery rate a page shows is ``clusters_addressed / clusters_total`` or a category\ncount over ``clusters_addressed``, both recomputable from the answer records.",
      "properties": {
        "category_counts": {
          "additionalProperties": {
            "type": "integer"
          },
          "title": "Category Counts",
          "type": "object"
        },
        "clusters_addressed": {
          "minimum": 0,
          "title": "Clusters Addressed",
          "type": "integer"
        },
        "clusters_total": {
          "minimum": 0,
          "title": "Clusters Total",
          "type": "integer"
        },
        "group_id": {
          "pattern": "^[a-z]{2,5}$",
          "title": "Group Id",
          "type": "string"
        },
        "sample_evidence": {
          "items": {
            "type": "string"
          },
          "title": "Sample Evidence",
          "type": "array"
        },
        "top_categories": {
          "items": {
            "type": "string"
          },
          "title": "Top Categories",
          "type": "array"
        },
        "top_category": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Top Category"
        },
        "top_category_tied": {
          "default": false,
          "title": "Top Category Tied",
          "type": "boolean"
        }
      },
      "required": [
        "group_id",
        "clusters_total",
        "clusters_addressed"
      ],
      "title": "GroupAnswerStats",
      "type": "object"
    },
    "LangText": {
      "additionalProperties": false,
      "description": "A free-text field with its language, per §1.6's hard rule.",
      "properties": {
        "lang": {
          "maxLength": 16,
          "minLength": 2,
          "title": "Lang",
          "type": "string"
        },
        "text": {
          "minLength": 1,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "text",
        "lang"
      ],
      "title": "LangText",
      "type": "object"
    },
    "Provenance": {
      "additionalProperties": false,
      "description": "``{skill_version, model_id, run_id, timestamp}`` (§3.2, §4.1).\n\n``model_id`` is ``None`` for purely deterministic producers (A1, scripts): recording\n\"no model was involved\" is itself an audit fact, and D10 makes it a meaningful one.",
      "properties": {
        "model_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Model Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "pattern": "^[A-Za-z0-9]+-\\d+\\.\\d+\\.\\d+$",
          "title": "Skill Version",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id",
        "timestamp"
      ],
      "title": "Provenance",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "One computed comparison outcome for one question.\n\n``summary`` is a deterministic English-pivot sentence assembled from the counts by\ncode — the write stage restates it for readers but may not change what it claims.\nA silence finding (``blindspot``, or ``attention_gap`` with ``total_silence``) must\ncarry the speaking side's anchors (silence needs contrast anchors), and its summary\nstates annotation-layer silence (\"no cluster was annotated as addressing…\"), never\nabsence from the world.",
  "properties": {
    "delta": {
      "anyOf": [
        {
          "$ref": "#/$defs/FindingDelta"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "finding_id": {
      "title": "Finding Id",
      "type": "string"
    },
    "groups": {
      "items": {
        "$ref": "#/$defs/GroupAnswerStats"
      },
      "maxItems": 2,
      "minItems": 2,
      "title": "Groups",
      "type": "array"
    },
    "interest": {
      "anyOf": [
        {
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Interest"
    },
    "kind": {
      "$ref": "#/$defs/FindingKind"
    },
    "merge_sensitive": {
      "default": false,
      "title": "Merge Sensitive",
      "type": "boolean"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "question_id": {
      "title": "Question Id",
      "type": "string"
    },
    "rank": {
      "minimum": 1,
      "title": "Rank",
      "type": "integer"
    },
    "secondary": {
      "default": false,
      "title": "Secondary",
      "type": "boolean"
    },
    "stability": {
      "anyOf": [
        {
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Stability"
    },
    "strength": {
      "$ref": "#/$defs/FindingStrength"
    },
    "summary": {
      "$ref": "#/$defs/LangText"
    },
    "thresholds_version": {
      "minLength": 1,
      "title": "Thresholds Version",
      "type": "string"
    },
    "topic_id": {
      "title": "Topic Id",
      "type": "string"
    },
    "total_silence": {
      "default": false,
      "title": "Total Silence",
      "type": "boolean"
    }
  },
  "required": [
    "finding_id",
    "topic_id",
    "question_id",
    "kind",
    "strength",
    "rank",
    "groups",
    "summary",
    "thresholds_version",
    "provenance"
  ],
  "title": "QAFinding",
  "type": "object"
}

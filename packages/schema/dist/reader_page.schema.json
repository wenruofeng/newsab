{
  "$comment": "GENERATED by newsab_schema.export — do not edit. Definition site: packages/schema/newsab_schema/ | blueprint value_chain.md The product | status: draft",
  "$defs": {
    "AngleBlock": {
      "additionalProperties": false,
      "description": "One storyline angle: a question, both sides' answers, the evidence.",
      "properties": {
        "caveat": {
          "anyOf": [
            {
              "$ref": "#/$defs/MultiLangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "commentary_joint": {
          "anyOf": [
            {
              "$ref": "#/$defs/PageClaim"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "detail": {
          "items": {
            "$ref": "#/$defs/PageClaim"
          },
          "title": "Detail",
          "type": "array"
        },
        "editorial_interest": {
          "anyOf": [
            {
              "$ref": "#/$defs/LangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "finding_id": {
          "title": "Finding Id",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/FindingKind"
        },
        "question_display": {
          "anyOf": [
            {
              "$ref": "#/$defs/MultiLangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "question_id": {
          "title": "Question Id",
          "type": "string"
        },
        "rank": {
          "minimum": 1,
          "title": "Rank",
          "type": "integer"
        },
        "shared_answer_label": {
          "anyOf": [
            {
              "$ref": "#/$defs/MultiLangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "sides": {
          "items": {
            "$ref": "#/$defs/SideAnswerBlock"
          },
          "maxItems": 2,
          "minItems": 2,
          "title": "Sides",
          "type": "array"
        }
      },
      "required": [
        "rank",
        "question_id",
        "finding_id",
        "kind",
        "sides"
      ],
      "title": "AngleBlock",
      "type": "object"
    },
    "ClaimType": {
      "description": "Three provenance paths, kept visually distinct on the page (§3.2, §4.5).\n\n``corpus_reading`` exists because the page must answer *what* the two sides say and\nnot only *how often* they say it.  That answer is read out of the source text; no A1\nmetric measures it.  Giving\nit its own type is how the cost stays visible: it may carry no ``computed_from`` and no\nquantifier, so it can never be mistaken on the page — or by a checker — for a number.",
      "enum": [
        "source_claim",
        "corpus_aggregate",
        "corpus_reading"
      ],
      "title": "ClaimType",
      "type": "string"
    },
    "CountBadge": {
      "additionalProperties": false,
      "description": "The clickable count (\"11 of 12 clusters\") behind one side's answer.\n\n``computed_from`` names the finding whose group stats these numbers recompute\nfrom; the pre-render checks re-derive them and refuse the page on mismatch.",
      "properties": {
        "computed_from": {
          "minLength": 1,
          "title": "Computed From",
          "type": "string"
        },
        "denominator": {
          "minimum": 0,
          "title": "Denominator",
          "type": "integer"
        },
        "group_id": {
          "pattern": "^[a-z]{2,5}$",
          "title": "Group Id",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "$ref": "#/$defs/MultiLangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "numerator": {
          "minimum": 0,
          "title": "Numerator",
          "type": "integer"
        }
      },
      "required": [
        "group_id",
        "numerator",
        "denominator",
        "computed_from"
      ],
      "title": "CountBadge",
      "type": "object"
    },
    "FindingKind": {
      "description": "The shapes a Q×A finding can take (value_chain.md \"The Q×A model\").\n\nSince the analyze refactor the emitted kinds are ``consensus`` /\n``divergence`` / ``attention_gap``, and a question emits at most one of them.  An\nattention gap (qa-0.4.0 semantics) asserts one side barely addresses the question\n(rate below the ``silent_max_rate`` threshold) while the other side addresses it\nsubstantially more — the blindspot-like reading; a mere rate difference between two\nsides that both plainly speak is not a finding.  It is first-class (it can carry an\nangle) and its angle always marks the quiet side; when the quiet side has zero\naddressed clusters the finding sets ``total_silence`` and is worded as\nannotation-layer silence.  ``blindspot`` and ``coverage_gap`` are no longer emitted\nbut stay members — historical runs on disk reference them (append-only).",
      "enum": [
        "consensus",
        "divergence",
        "attention_gap",
        "blindspot",
        "coverage_gap"
      ],
      "title": "FindingKind",
      "type": "string"
    },
    "HowWeCounted": {
      "additionalProperties": false,
      "description": "The methodology appendix block — the only place run IDs may appear.",
      "properties": {
        "answers_run_id": {
          "minLength": 1,
          "title": "Answers Run Id",
          "type": "string"
        },
        "corpus_run_id": {
          "minLength": 1,
          "title": "Corpus Run Id",
          "type": "string"
        },
        "notes": {
          "items": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Notes",
          "type": "array"
        },
        "qa_run_id": {
          "minLength": 1,
          "title": "Qa Run Id",
          "type": "string"
        },
        "questions_run_id": {
          "minLength": 1,
          "title": "Questions Run Id",
          "type": "string"
        }
      },
      "required": [
        "corpus_run_id",
        "questions_run_id",
        "answers_run_id",
        "qa_run_id"
      ],
      "title": "HowWeCounted",
      "type": "object"
    },
    "LangText": {
      "additionalProperties": false,
      "description": "A free-text field with its language, per §1.6's hard rule.",
      "properties": {
        "lang": {
          "maxLength": 16,
          "minLength": 2,
          "title": "Lang",
          "type": "string"
        },
        "text": {
          "minLength": 1,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "text",
        "lang"
      ],
      "title": "LangText",
      "type": "object"
    },
    "MultiLangText": {
      "additionalProperties": false,
      "description": "A label carried in several languages at once (topic titles, concept labels).",
      "properties": {
        "values": {
          "additionalProperties": {
            "type": "string"
          },
          "minProperties": 1,
          "title": "Values",
          "type": "object"
        }
      },
      "required": [
        "values"
      ],
      "title": "MultiLangText",
      "type": "object"
    },
    "PageClaim": {
      "additionalProperties": false,
      "description": "One reader-facing sentence with its provenance path (§4.5, inline form).\n\n``source_claim``: someone said this — anchors point at where.\n``corpus_aggregate``: our sample counts like this — ``computed_from`` names the\nfinding/stat path the number recomputes from, anchors illustrate.\n``corpus_reading``: we read the coverage and this is what it says — ≥ 2 anchors,\nno number, no ``computed_from``.",
      "properties": {
        "claim_type": {
          "$ref": "#/$defs/ClaimType"
        },
        "computed_from": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Computed From"
        },
        "evidence": {
          "items": {
            "type": "string"
          },
          "title": "Evidence",
          "type": "array"
        },
        "text": {
          "$ref": "#/$defs/MultiLangText"
        }
      },
      "required": [
        "text",
        "claim_type"
      ],
      "title": "PageClaim",
      "type": "object"
    },
    "Provenance": {
      "additionalProperties": false,
      "description": "``{skill_version, model_id, run_id, timestamp}`` (§3.2, §4.1).\n\n``model_id`` is ``None`` for purely deterministic producers (A1, scripts): recording\n\"no model was involved\" is itself an audit fact, and D10 makes it a meaningful one.",
      "properties": {
        "model_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Model Id"
        },
        "run_id": {
          "minLength": 1,
          "title": "Run Id",
          "type": "string"
        },
        "skill_version": {
          "pattern": "^[A-Za-z0-9]+-\\d+\\.\\d+\\.\\d+$",
          "title": "Skill Version",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        }
      },
      "required": [
        "skill_version",
        "run_id",
        "timestamp"
      ],
      "title": "Provenance",
      "type": "object"
    },
    "Quote": {
      "additionalProperties": false,
      "description": "A verbatim quote reference: the sentence ID plus reader-language translations.\n\nThe original text is resolved from the corpus at render time — URL + verbatim\nsentence is the public form (non-negotiable 7).",
      "properties": {
        "sentence_id": {
          "title": "Sentence Id",
          "type": "string"
        },
        "translation": {
          "anyOf": [
            {
              "$ref": "#/$defs/MultiLangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "sentence_id"
      ],
      "title": "Quote",
      "type": "object"
    },
    "ReaderLexicon": {
      "additionalProperties": false,
      "description": "Reader wording for the machine vocabulary the page displays.\n\nThree vocabularies reach the reader that were never written for one:\n\n* **question wording** — the annotate stage writes questions for an annotator\n  (\"What is presented as the problem — what the new visa rules respond to, or the\n  rules themselves?\"). Those qualifiers make the annotation reproducible and make the\n  question unreadable. The reader gets the same question asked plainly (\"在整个事件\n  里，问题出在哪儿？\"); it must ask the same thing, never a different or narrower one.\n* **answer categories** — ``international_students_generally`` is a counting key, not\n  a phrase. Every category the page displays needs a short reader label.\n* **topics raised** — collect stores a source phrase plus an English pivot.  The pivot\n  is the stable cross-language concept key, not Chinese reader copy; render/localize\n  translates that concept instead of reverse-looking-up the article's wording.\n\nBoth are written once per page and localized with the rest of it, so the storyline,\nthe answer cards and the annotation data view all say the same words. Entries the page\ndoes not carry fall back to the annotation wording (questions) or the raw key\n(categories) — a missing entry degrades the page, it never fabricates one.",
      "properties": {
        "categories": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Categories",
          "type": "object"
        },
        "group_definitions": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Group Definitions",
          "type": "object"
        },
        "group_labels": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Group Labels",
          "type": "object"
        },
        "group_short_labels": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Group Short Labels",
          "type": "object"
        },
        "questions": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Questions",
          "type": "object"
        },
        "scope": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Scope",
          "type": "object"
        },
        "topics": {
          "additionalProperties": {
            "$ref": "#/$defs/MultiLangText"
          },
          "title": "Topics",
          "type": "object"
        }
      },
      "title": "ReaderLexicon",
      "type": "object"
    },
    "SideAnswerBlock": {
      "additionalProperties": false,
      "description": "One side of one angle: the plain-language answer, its quotes, its badge.\n\n``answer_label`` is the three-to-eight-word form of the answer the renderer puts on\nthe answer card *above* the two side blocks, so a reader meets \"what this coverage\nanswers\" before meeting the paragraph explaining it.  It is short reader\nlanguage — the writer's job — but ``answer_category`` binds it to the counted\ncategory underneath, and the checks refuse a label whose category does not recompute\nfrom the finding.",
      "properties": {
        "answer": {
          "$ref": "#/$defs/PageClaim"
        },
        "answer_category": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Answer Category"
        },
        "answer_label": {
          "anyOf": [
            {
              "$ref": "#/$defs/MultiLangText"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "badge": {
          "$ref": "#/$defs/CountBadge"
        },
        "group_id": {
          "pattern": "^[a-z]{2,5}$",
          "title": "Group Id",
          "type": "string"
        },
        "is_silent_side": {
          "default": false,
          "title": "Is Silent Side",
          "type": "boolean"
        },
        "quotes": {
          "items": {
            "$ref": "#/$defs/Quote"
          },
          "title": "Quotes",
          "type": "array"
        }
      },
      "required": [
        "group_id",
        "answer",
        "badge"
      ],
      "title": "SideAnswerBlock",
      "type": "object"
    },
    "Visual": {
      "additionalProperties": false,
      "description": "A data visual that carries story weight (never decoration).\n\nRendered deterministically from the named analysis artifact; obeys the same\ndiscipline as sentences — an unsupported contrast may not be drawn (V-3).\n\n``concept_cloud`` is the one kind that is not about a question: it sums every side's\n``category_counts`` across the whole question set and draws the two answer\nvocabularies side by side.  It asserts nothing and compares nothing — two raw\ndistributions, normalized by each side's own total — so it carries no ``question_id``\nand V-3 has nothing to bite on.",
      "properties": {
        "caption": {
          "$ref": "#/$defs/MultiLangText"
        },
        "data_from": {
          "minLength": 1,
          "title": "Data From",
          "type": "string"
        },
        "kind": {
          "pattern": "^(answer_distribution|addressed_rates|coverage_timeline|concept_cloud)$",
          "title": "Kind",
          "type": "string"
        },
        "question_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Question Id"
        }
      },
      "required": [
        "kind",
        "caption",
        "data_from"
      ],
      "title": "Visual",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "The whole reader page for one topic (value_chain \"The product\").",
  "properties": {
    "angles": {
      "items": {
        "$ref": "#/$defs/AngleBlock"
      },
      "maxItems": 6,
      "minItems": 1,
      "title": "Angles",
      "type": "array"
    },
    "hook": {
      "anyOf": [
        {
          "$ref": "#/$defs/PageClaim"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "how_we_counted": {
      "$ref": "#/$defs/HowWeCounted"
    },
    "intro": {
      "items": {
        "$ref": "#/$defs/PageClaim"
      },
      "maxItems": 6,
      "minItems": 1,
      "title": "Intro",
      "type": "array"
    },
    "lexicon": {
      "$ref": "#/$defs/ReaderLexicon"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "title": {
      "$ref": "#/$defs/MultiLangText"
    },
    "topic_id": {
      "title": "Topic Id",
      "type": "string"
    },
    "visuals": {
      "items": {
        "$ref": "#/$defs/Visual"
      },
      "title": "Visuals",
      "type": "array"
    }
  },
  "required": [
    "topic_id",
    "title",
    "intro",
    "angles",
    "how_we_counted",
    "provenance"
  ],
  "title": "ReaderPage",
  "type": "object"
}
